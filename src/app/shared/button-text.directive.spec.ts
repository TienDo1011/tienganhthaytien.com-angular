import { ButtonTextDirective } from './button-text.directive';

describe('ButtonTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonTextDirective();
    expect(directive).toBeTruthy();
  });
});
